---
layout: post
title: cry（hubu
date: 2024-12-16 15:13:32 +0800
categories: [crypto]
---

密码学

古典密码：（**代换和置换**）
- 单标替换加密 eg：凯撒，移位，atbash，ROT
        暴力破解
        词频分析  http://quipqiup.com/   
- 多表替换加密 eg：维吉尼亚，棋盘，希尔hill，关键字
- 奇奇怪怪的加密方式

	凯撒，维吉尼亚
	栅栏密码（改变明文顺序）
	        eg：去掉空格，取几个为一组（就是栏数），然后一排排放置，取出列然后成为密文
	ROT:凯撒密码只是rot的一种特例
	 rot13只对英文字母，字母表循环移动13个字符
	 rot47针对ASCII，对ASCII（32-126）循环移动47位

     维吉尼亚：使用一系列凯撒密码组成的密码字母表来加密
              字母频率
              key长度不够会自己复制粘贴扩展延长
              https://www.gubalia
     
现代密码学
- 对称加密 eg:DES,AES,RC4
- 非对称（题目比较多）eg: <mark>RSA</mark>,ELGamal，椭圆曲线加密
- 哈希
- 数字签名
- 分组
- 序列密码

    AES：加解密用相同的密钥
     CBC（让相同的明文得到不同的密文）
         采用异或，加密完给那个啥前面的异或
         iv是随着密文传送过去的，本来就是第一块的话就没有前一块密文，就与iv异或
         
     ![]({{ site.baseurl }}/assets/images/e5c0d4182c4db3b32f230e16476468a4_720.png)
     ECB（电子密码本，但是相同的明文得到相同的密文）
     
    RSA:
      公钥和私钥
      随机选择两个不同大的质数p和q，计算n=p×q
      根据欧拉函数，求得φ(n)=φ(q)φ(p)
      消息加密c亖m^e(，解密m亖c^d
      `c = pow(m, e, n)`  计算 c=m^e(mod  n)来加密消息，结果存储在 `c` 中。
    

#### Strang King

![]({{ site.baseurl }}/assets/images/Pasted image 20241110091628.png)

#### helloAES
    pad(flag, AES.block_size) 是用于对称加密算法 AES 中数据填充的操作。由于 AES 的块大小固定为 16 字节，pad 函数确保输入数据的长度为该块大小的倍数。如果数据长度不是 16 字节的倍数，通常会使用 PKCS#7 填充方式，为数据末尾添加必要的字节，以使总长度符合要求。

去掉填充：
    from Crypto.Util.Padding import unpad  

    unpadded_data = unpad(padded_data, block_size)  
    print(unpadded_data)  # 将输出原始的明文
    
![]({{ site.baseurl }}/assets/images/Pasted image 20241110094854.png)

#### 不用谢喵

CBC
![]({{ site.baseurl }}/assets/images/ef64d5cd7b2c4858bea1335292004d9e.webp)

ECB
![]({{ site.baseurl }}/assets/images/248bee0c6fba4a6884d9df430a553bb7.webp)


```python
from Crypto.Cipher import AES
from Crypto.Util.number import *
import os
 
KEY = b"fake_key_fake_ke"
FLAG = "flag{fake_flag_fake_flag}"  
 
def decrypt(c):
    AES_ECB = AES.new(KEY, AES.MODE_ECB) #ECB加密
    decrypted = AES_ECB.decrypt(long_to_bytes(c))
 
    return decrypted.hex()
 
def encrypt():
    iv = os.urandom(16)
    AES_CBC = AES.new(KEY, AES.MODE_CBC, iv)#CBC加密
    encrypted = AES_CBC.encrypt(FLAG.encode())
    print('iv:',iv.hex())
 
    return iv.hex() + encrypted.hex()
 
c=encrypt()
print('encrypt:',c)
print('decrypt:',decrypt(int(c,16)))
 
encrypt: f2040fe3063a5b6c65f66e1d2bf47b4c ddb206e4ddcf7524 932d25e92d57d346 8398730b59df851c bac6d65073f9e138
什么是AES啊😭，求求你帮我解密吧，我什么都会做的！！！！！😭
 
'''
什么都会做？那就去学习一下AES吧……
我这次就先给你解一下好了，不用谢喵
        #f2040fe3063a5b6c65f66e1d2bf47b4c   ddb206e4ddcf7524932d25e92d57d346   8398730b59df851cbac6d65073f9e138
decrypt: f9899749fec184d81afecd35da430bc3   94686e847d72141b3a955a4f6e920e7d   91cb599d92ba2a6ba51860bb5b32f23b
这对吗？哦不对不对，哦对的对的。
'''
```


AES一般是分成16个字节一块一块的加密

知道第二块的密文解密后的明文，xor(decrypt[-32:])
encrypt_cbc[32:64]
第一块明文是xor（decrypted

```python
from pwn import xor

encrypted_cbc = "f2040fe3063a5b6c65f66e1d2bf47b4cddb206e4ddcf7524932d25e92d57d3468398730b59df851cbac6d65073f9e138"
IV = bytes.fromhex(encrypted_cbc[:32])

c1 = bytes.fromhex(encrypted_cbc[32:64])
c2 = bytes.fromhex(encrypted_cbc[64:96])

decrypted_ecb = "f9899749fec184d81afecd35da430bc394686e847d72141b3a955a4f6e920e7d91cb599d92ba2a6ba51860bb5b32f23b"

m2 = xor(bytes.fromhex(decrypted_ecb[-32:]), c1)
m1 = xor(bytes.fromhex(decrypted_ecb[32:64]), IV)

print(m1 + m2)
```


#### 推开RSA的神秘之门

![]({{ site.baseurl }}/assets/images/Pasted image 20241110145303.png)


#### [RSA]P0

    `n = getPrime(1024)`：这一行生成一个 1024 位的随机质数，并将其赋值给变量 `n`。
    那么求phin直接phin=n-1（细节不清楚还得看ppt

![]({{ site.baseurl }}/assets/images/Pasted image 20241110152319.png)

#### [RSA]P1

**<mark>小明文攻击</mark>**
n很大 n=q* p，暴力分解不可能
如果m很小则c=m^e
可以不取模，模这里不影响
直接开方就可以得到

用gmpy2的iroot开任意次方
用法：iroot（10,3）及对10开3次方
（mpz（2），false）
第一项为结果，第二项是
这里一般写为gmpy2.iroot（c,e)
    gmpy2.iroot(c,e)[0] 一般是取第一个


![]({{ site.baseurl }}/assets/images/Pasted image 20241110153609.png)

#### [RSA]P2
     由于个人直接iroot出来了还以为都是一个类型（悲

<mark>**低加密指数攻击，**</mark>
m^e和n差不多大小，e通常很小
遍历k

![]({{ site.baseurl }}/assets/images/Pasted image 20241110153847.png)

wp：
```python

from Crypto.Util.number import long_to_bytes
import gmpy2

n = 
e = 3
c = 
k = 0
while True:
    if gmpy2.iroot(c + k * n, e)[1]:
        m = gmpy2.iroot(c + k * n, e)[0]
        print(long_to_bytes(m))
        break
    k += 1
```

#### [RSA]P3
    同理，n=q* p太大，直接开方，emm个人这么直接的想的然后爆出来了。。

**-<mark>Rabin算法</mark>**
e特别小，但是e是偶数，c不是一个完全平方数
不能用低加密指数

取两个大素数p和q
二次剩余和什么准则

Rabin加密是RSA的衍生算法,
e=2是Rabin加密的典型特征,
加密过程可以表示为`c = m^2 mod n`, 
解密过程为`m = c^(1/2) mod n`

![]({{ site.baseurl }}/assets/images/Pasted image 20241110154115.png)

wp：
```python
# Rabin 算法, e = 2, gcd(e, phin) != 1

from Crypto.Util.number import *
import gmpy2

p = 58693358221457302930460756375595859186524580663448139925121852924221737371479
q = 67999001041542810668782743474695919662164908250816321788046853027703332430299
e = 2
c = 3802870480414123652120929841667412561256376831403467185036448573196304271500279139613477098552264387553730676051464537288506865495393527561

def rabin_attack(c, p, q):
   n = p * q
   b1 = pow(c, (p + 1) // 4, p)
   b2 = pow(c, (q + 1) // 4, q)

   s = gmpy2.invert(q, p)
   t = gmpy2.invert(p, q)

   m1 = (b1 * s * q + b2 * t * p) % n
   m2 = (b1 * s * q + (q - b2) * t * p) % n
   m3 = ((p - b1) * s * q + b2 * t * p) % n
   m4 = ((p - b1) * s * q + (q - b2) * t * p) % n

   return m1, m2, m3, m4

ms = rabin_attack(c, p, q)

for m in ms:
   print(long_to_bytes(m))
```

#### [RSA]P4

e很大
φ(n)=()()=n-p-q+1
约等于n
Wiener攻击
e/φ（n）-



wp：
```python
# Wiener攻击    e 很大，d 很小

from Crypto.Util.number import *
from gmpy2 import *

n = 8787001376889916299102201724037536344402854158008669959010443728199826652967617674114618684728515435409573908348037886340474245642726777122772261940042237
e = 5429576341927495021973651006157912114534898931999824774515353175299725851790026015231318807001775606396837409762765933126694125657322898520046173036939607
c = 6779461226114864249905011938567661389461933701344521006059173750831481072580368488497672341116718568112315528001578328290458672169470685366464764125653523

class ContinuedFraction():
    def __init__(self, numerator, denumerator):
        self.numberlist = []  # number in continued fraction
        self.fractionlist = []  # the near fraction list
        self.GenerateNumberList(numerator, denumerator)
        self.GenerateFractionList()

    def GenerateNumberList(self, numerator, denumerator):
        while numerator != 1:
            quotient = numerator // denumerator
            remainder = numerator % denumerator
            self.numberlist.append(quotient)
            numerator = denumerator
            denumerator = remainder

    def GenerateFractionList(self):
        self.fractionlist.append([self.numberlist[0], 1])
        for i in range(1, len(self.numberlist)):
            numerator = self.numberlist[i]
            denumerator = 1
            for j in range(i):
                temp = numerator
                numerator = denumerator + numerator * self.numberlist[i - j - 1]
                denumerator = temp
            self.fractionlist.append([numerator, denumerator])


a = ContinuedFraction(e, n)
for k, d in a.fractionlist:
    m = powmod(c, d, n)
    flag = long_to_bytes(m)
    
    if b'HUBUCTF' in flag:
        print(flag)
```




#### [RSA]P5

低加密指数广播攻击
ci=m
m^e亖∑i=1 n citiMi(mod N)


wp：
```python
from pwn import *
from Crypto.Util.number import *
import gmpy2

context.log_level = 'debug'

r = remote("challenge.hubuctf.cn", 30126)

def crt(n_list, c_list):
    N = 1
    for n in n_list:
        N *= n
    result = 0
    for n, c in zip(n_list, c_list):
        m = N // n
        result += c * m * inverse(m, n)
    return result % N

n_list = []
c_list = []

for _ in range(64):
    r.sendlineafter("input> ", "1")
    n_list.append(int(r.recvline().strip().split(b": ")[1]))
    c_list.append(int(r.recvline().strip().split(b": ")[1]))

m = gmpy2.iroot(crt(n_list, c_list), 127)[0]    # 127 is the e

print(long_to_bytes(m))
```


![]({{ site.baseurl }}/assets/images/Pasted image 20241110165918.png)


#### [RSA]P6

p-1光滑攻击
当一个数的最大质因数不大于b时，

费马小定理
a^p-1 亖1（mod p）










p+1光滑攻击
