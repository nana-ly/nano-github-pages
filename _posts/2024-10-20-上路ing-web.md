---
layout: post
title: 上路ing（web
date: 2024-10-20 16:47:01 +0800
categories: [web]
---

##### weak_auth，
    意思是弱密码
开始注册页面，随便输一个用户名和密码，这里试的是123,123
    用F12也没看出什么
这里登出一个提示说please login as admin
开始没管这个，后来才知道这个也是一个提示，用户名可能是admin
所以可以尝试把用户名改为admin，密码随便我就没改
然后登出password error这种就是密码错误，所以下面应该找正确密码
      这里后面好像一直是白屏，没什么值得注意
再用F12发现出现you may need a dictionary这样的字样

需要字典？再加上前面提到的弱密码，应该是要破解密码了
打开burpsuite，可以用lntruder（破解模块）
用burpsuite抓包，然后发送到intruder里

![]({{ site.baseurl }}/assets/images/屏幕截图 2024-08-19 171821 1.png)
这里是弄出字典载入的方式，当时找了很久，发现电脑还是有字典的
![]({{ site.baseurl }}/assets/images/屏幕截图 2024-08-19 171505.png)
![]({{ site.baseurl }}/assets/images/屏幕截图 2024-08-19 171530.png)
载入后开始攻击
![]({{ site.baseurl }}/assets/images/屏幕截图 2024-08-19 171748.png)

出现类似这种（这里是找的图，不是截的，当时忘了都是差不多）
找到长度不一样的那个payload，那个就是正确的
这里是找的密码，所以那个不一样的payload就代表之前的密码
修改密码，然后可得flag
![]({{ site.baseurl }}/assets/images/Pasted image 20240819180147.png)



##### disabled_button
 不能按的按钮

开始出现一个按不了的按钮，看F12里面可以看出一些奇怪
后面慢慢知道class=”btn btn-default“是指设置按钮
所以这里发现前面加了disabled，那么应该就是它为阻碍，所以尝试把它删掉
![]({{ site.baseurl }}/assets/images/屏幕截图 2024-08-19 161410.png)
 ![]({{ site.baseurl }}/assets/images/屏幕截图 2024-08-19 160507.png)
删掉发现可以按按钮，然后可得flag

##### PHP2
<mark>存在一个后缀名为.phps的文件</mark>  
`后缀名为.phps的文件出现在无法使用web浏览器查看php源代码的情况下，.phps文件就是对应文件的源代码！`
但这里肯定与php有关，题目在php后面加了个2，想到phps, phps 文件就是 php 的源代码文件
通过访问index.phps，我们得到php的源码

URL编码原理：将每个字母对应转换成16进制，且每个前面加％
我们可以看到URL默认对字母和数字不编码，这里第一次对字母或者数字的编码需要手动编码

按照上述方法，我们先将字母转成16进制，再都加上％得到：%61%64%6d%69%6e
浏览器会自动进行一次URL编码的转换  

这里还有一个问题就是，我们在浏览器输入admin时，浏览器本身就会对admin进行一次url解码，所以这里需要对admin进行二次url编码
第二次就可以直接使用在线编码工具了

那么再进行一次URL编码：
当然我们也可以根据它的原理自己编码，因为％对应的16进制为25，而url默认对数字不会进行编码，所以只需要将％都换成25，且每个前面再加上％即可。

##### Web_php_include

strstr()：
定义和用法：
搜索字符串在另一个字符串中是否存在，如果是，返回字符串及剩余部分，否则返回false。
区分大小写，stristr()函数不区分大小写
语法：
strstr(string,search,before_search)
string:必需，被搜索的字符串
search:必需，要搜索的字符串，若是数字，则搜索对应的ASCII值的字符
before_search：可选，默认为“false”，若为true,将返回search参数第一次出现之前的字符串部分

str_replace():
定义和用法：
以其它字符替换字符串中的一些字符（区分大小写）
语法：
str_replace(find,replace,string,count)
find,必需，要查找的值
replace，必需，要替换的值
string，必需，被搜索的字符串
count，可选，替换次数

程序过滤掉了page=参数传入php://
利用php伪协议大小写绕过





#### command_execution

**ping命令**
    
	 ping 命令是一个常用的网络命令，用于测试两台计算机之间网络连接的连通性。通过向目标计算机发送 ICMP 协议的数据包，并等待目标计算机返回响应数据包来测试网络的可达性，同时也可以测试网络的延迟和丢包率等信息。

在终端中使用 ping 命令的**基本语法**为：

```dos
ping <IP地址或主机名>
```

例如，要测试目标计算机的 IP 地址为 192.168.1.1 的网络连通性，可以运行以下命令：

```dos
ping 192.168.1.1
```

ping 命令将会输出一些关于网络连接的信息，包括每个数据包的传输时间、接收时间和延迟时间等。如果目标计算机能够正确响应 icmp 数据包，那么 ping 命令将会显示类似以下的信息：
```vbnet
ping: connect: Network is unreachable
```

这表示目标计算机成功返回了一个数据包，并花费了 4.56 毫秒的时间。如果无法连接到目标计算机，则 ping 命令将会显示类似以下的信息：

```vbnet
ping: connect: Network is unreachable
```

这表示无法连接到目标计算机。
可以根据 ping 命令输出的信息排查网络故障，以便进行调试和修复网络连接问题。

 **ping命令的应用格式**

1. ping+IP地址或主机域名
2. ping+IP地址或主机域名+命令参数
3. ping+命令参数+IP地址或主机域名  
    **注意，+要换成空格**

命令执行漏洞（| || & && 称为 管道符）

<mark>&&</mark>（命令执行控制） 语法格式如下：
command1 && command2 [&& command3 ...]    
1 命令之间使用 && 连接，实现逻辑与的功能。 
 2 只有在 && 左边的命令返回真（命令返回值 $? == 0），&& 右边的命令才会被执行。 
  3 只要有一个命令返回假（命令返回值 $?== 1），后面的命令就不会被执行。

<mark>||</mark>（命令执行控制） 语法格式如下：
command1 || command2 [|| command3 ...]    
1 命令之间使用 || 连接，实现逻辑或的功能。  
2 只有在 || 左边的命令返回假（命令返回值 $? == 1），|| 右边的命令才会被执行。这和 c语言中的逻辑或语法功能相同，即实现短路逻辑或操作。  
3 只要有一个命令返回真（命令返回值 $? == 0），后面的命令就不会被执行。

<mark>|</mark> 常用的管道符号，前面命令的执行结果作为后面结果的输入 管道和重导向：“|”、“>”、“>>”、“<”
重导向就是使命令改变它所认定的标准输出。“>”可将结果输出到文件中，该文件原有内容会被删除，“>>”则将结果附加到文件中，原文件内容不会被删除。“<”可以改变标准输入。
如：
cat data1.txt>>data2.txt(将data1.txt文件的内容加在data2.txt文件的后面)
管道“|”可将命令的结果输出给另一个命令作为输入之用.

连接符号：“;” 当有几个命令要连续执行时，我们可以把它们放在一行内，中间用“;”分开。 mkdir myfile;cp
/tmp/myfile.txt myfile(先建立一个目录myfile，然后把myfile.txt拷贝到新建的目录中)

后台执行：“&”
用户有时候执行命令要花很长时间，可能会影响做其他事情。最好的方法是将它放在后台执行。后台运行的程序在用户注销后系统还可以继续执行。当要把命令放在后台执行时，在命令的后面加上“&”。




aa || ls
aa || find / -iname "flag"



#### [MoeCTF 2021]babyRCE

过滤了很多命令
（我这个很奇怪好像只能用?rce=ta\c%09f\lag.php，cat搞不了特别奇怪
