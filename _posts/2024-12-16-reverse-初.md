---
layout: post
title: reverse（初
date: 2024-12-16 15:16:50 +0800
categories: [Reverse]
---

#### Reversing-x64Elf-100
     攻防世界

    '''  
    主函数main  
    __int64 __fastcall main(int a1, char **a2, char **a3)  
    {  
    char s[264]; // [rsp+0h] [rbp-110h] BYREF  unsigned __int64 v5; // [rsp+108h] [rbp-8h]  //这里就是定义一些函数  
      v5 = __readfsqword(0x28u);  
     printf("Enter the password: "); //与C语言一样  
     if ( !fgets(s, 255, stdin) ) //我们猜测这里的“s”就是flag，然后对下面的sub_4006FD函数操作flag  
    return 0LL;  if ( (unsigned int)sub_4006FD(s) )//这里是一个判断，成功则输出nice，失败输出Incorrect password!  
  //，我们可以推测处flag在这里面，双击该函数sub_4006FD  
  {    puts("Incorrect password!");    return 1LL;  }  else  {    puts("Nice!");    return 0LL;  }}
'''

定义了一个264位的字符串s，这个应该是执行程序后输入的字符串，然后会通过如下语句进行输入提示字符串printf("Enter the password: "); ，然后读取字符串然后发送到sub_4006FD函数进行校验，如果校验失败提示Incorrect password!，校验成功返回nice。

![]({{ site.baseurl }}/assets/images/屏幕截图 2024-10-27 142423.png)
可以看到代码如图所示，v3是一个二维数组，v3[][],，然后通过for循环，0<i<12,如果校验算出的值比我们输入的值大于1，则通过校验，返回0LL，否则校验失败，返回1LL。  
校验算法为v3数据第一个参数为i%3，第二个参数为[2*(i/3)],这里的i/3是c语言中的，取得整，然后通过ascll码减一再转为字符串得到我们的flag。

    
    __int64 __fastcall sub_4006FD(__int64 a1)  
     
     {  
     int i; // [rsp+14h] [rbp-24h]  __int64 v3[4]; // [rsp+18h] [rbp-20h]   
     v3[0] = (__int64)"Dufhbmf";  
     v3[1] = (__int64)"pG`imos";  
     v3[2] = (__int64)"ewUglpt";  //这儿可以看成是一个二维数组 char v3[0][7]    
      
    for ( i = 0; i <= 11; ++i ) //进入for循环，通过i <= 11我们可以推测处flag的长度为12  
    {    if ( *(char *)(v3[i % 3] + 2 * (i / 3)) - *(char *)(i + a1) != 1 )      
    //这儿的代码逻辑是v3字符串-a1字符串不等于1（我自己的理解），则返回1；  
       //这里的a1就是flag了，可以看出我们上面分析的s是flag是正确的  
      //a1[i]=v[i % 3][2 * (i / 3)]-1      //写wp的时候要注意(i / 3)要返回一个整数  
       
      return 1LL;  
    }  return 0LL;}  

*(char *)(v3[i % 3] + 2 * (i / 3)) - 1 = *(char *)(i + a1)  
v3[i % 3] + 2 * (i / 3)这是一个数组，减去i + a1这个数组，等于1
a1=v3[i%3]+2*(i/3)-1

a1[i]=v3【i%3】【2*(i/3)】-1
这里可以看出v3是二维数组
因为是循环，每次输出a1【i】，要想全部输出，就是输出完整的flag，就自己加自己
就是用+=
或者
flag =chr(ord(v3[i%3][(2*int(i/3))]) -1) + flag  这种

这里a1就是flag  （v3，i都是已知）
  
<mark>前边有*（char*）意思是后边的是数组</mark>  
==所以是（i+a1）=a1[i]==  
'''

脚本
    k=["Dufhbmf","pG`imos","ewUglpt"]  
    flag=""  
  
    for i in range(12):  
    flag+=chr(ord(k[i%3][(2*int(i/3))]) -1)  
    print(flag)  


    ord() 函数是 chr() 函数（对于8位的ASCII字符串）或 unichr() 函数（对于Unicode对象）的配对函数，它以一个字符（长度为1的字符串）作为参数，返回对应的 ASCII 数值，或者 Unicode 数值，
        如果所给的 Unicode 字符超出了你的 Python 定义范围，则会引发一个 TypeError 的异常。
        
总之就是写脚本好像都要带这种头衔，chr，ord，int这种应该是很常见的

#### Begin
     NewStar2024
     wp主要是帮助初步使用IDA，很细节的那种

首先打开运行一下程序
发现是提示欸
![]({{ site.baseurl }}/assets/images/Pasted image 20241030190325.png)

这里浅浅用exeinfo查一下，64位无壳
![]({{ site.baseurl }}/assets/images/Pasted image 20241030190525.png)

题目说flag由3个部分组成

打开IDA，跟着指示按F5，看到主函数
![]({{ site.baseurl }}/assets/images/Pasted image 20241031162924.png)
追踪flag_part1（可以戳开）
根据指示按a然后可以看到flag_part1具体内容
![]({{ site.baseurl }}/assets/images/屏幕截图 2024-10-31 163142.png)

跟着提示说想得到第二部分按shift+F12
![]({{ site.baseurl }}/assets/images/Pasted image 20241031163612.png)
得到第二部分
根据提示，在 Format 上按下 X 查看是谁引用了这个字符串
再根据提示，在函数名字上按下x键找到是哪个函数引用了这个函数
![]({{ site.baseurl }}/assets/images/屏幕截图 2024-10-31 163940.png)

![]({{ site.baseurl }}/assets/images/Pasted image 20241031164054.png)

得到第三部分
还说别忘加}
他真的我哭死

拼起来得到flag
#### Base64
     NewStar2024
     wp细节哇

开始运行一下，要输入flag
随便输了个，然后输出wrong什么的然后就没了

![]({{ site.baseurl }}/assets/images/Pasted image 20241031165845.png)

按 ⇧ ShiftF12 调出字符串界面
![]({{ site.baseurl }}/assets/images/Pasted image 20241031170459.png)
发现有两串好像base64

先戳进去这个输入flag的开头
然后用F5看到函数

双击 `DATA XREF` 右边的地址加偏移，就可以到引用他的地方，按下 F5，即可反编译这段逻辑
![]({{ site.baseurl }}/assets/images/Pasted image 20241031170843.png)

if ( strlen(Str) == 26 && (sub_1400014E0(Str, 26LL, Str1), !strcmp(Str1, "g84Gg6m2ATtVeYqUZ9xRnaBpBvOVZYtj+Tc=")) )
  {
    sub_140001450("Correct flag!\n");
    sub_140001450("Welcome to NewStar!");
  }
  else
  {
    sub_140001450("Wrong flag, try again.\n");
  }

应该是长度要等于26而且要和g84Gg6m2ATtVeYqUZ9xRnaBpBvOVZYtj+Tc=这串相等
才行
把这串类似base64的拿去厨子一下
发现乱码
在这个后面看见了这一串字符
![]({{ site.baseurl }}/assets/images/Pasted image 20241031172105.png)
wp说是换表了
把那串给厨子换上，得到flag
![]({{ site.baseurl }}/assets/images/屏幕截图 2024-10-31 172600.png)




<mark>安卓逆向用jadx打开apk</mark>

#### ezAndroidStudy
     跟着wp走
     

查看 `AndroidManifest.xml` 可以发现 activity 只有 `Homo` 和 `MainActivity`

我们用 Jadx 打开 `work.pangbai.ezandroidstudy.Homo` 就可以获得 flag1

![]({{ site.baseurl }}/assets/images/Pasted image 20241031181220.png)

打开 `resources.arsc/res/value/string.xml` 搜索 flag2 即可



    安卓卓用到的可不只有 java 呢，你知道怎么逆向安卓的 so 文件吗喵
    一个 Activity 就是一个页面呢，每一次页面切换，就是 Activity 的切换。
    问题来了，我们怎么才能知道一个软件有哪些 Activity 呢？答案啊，是查找     AndroidManifest.xml 文件里的 Activity 标签呢，你能在 Activity 里找到第一段 flag 吗喵？
    你知道吗？apk 中不用编译的资源(一下其他类型的文件)通常放在 /assets 目录和 /res/raw          目录下呢，你能找到第四段 flag 吗喵？(悄悄告诉你，这个比赛很容易考到呢)

到 `/layout/activity_main.xml` 找即可

`/res/raw/flag4.txt` 里

IDA64 打开 `lib/x86_64/libezandroidstudy.so`，找到以 Java 开始的函数，按 F5 即可




ez_debug

开始运行一下，输入flag，随便输了下，直接闪退
![]({{ site.baseurl }}/assets/images/Pasted image 20241031215300.png)


Decrypted解密
