---
layout: post
title: HTTP请求头相关
date: 2024-12-11 22:12:11 +0800
categories: [web]
---

[HTTP中POST提交数据的四种方式详解 - 知乎](https://zhuanlan.zhihu.com/p/344743820)

**HTTP 方法**
- _GET_
- _POST_
- _PUT_
- _HEAD_
- _DELETE_
- _PATCH_
- _OPTIONS_
最常用的两种方法是：GET 和 POST。

GET:到  
Host:来自  
User-Agent: 用户-代理  
Accept: 接受  
Accept-Language: 接受-语言  
Accept-Encoding: 接受-编码  
Connection: 连接  
Upgrade-Insecure-Requests: 升级-不安全的-请求  
Content-Length: 内容长度  
Cache-Control: 缓存-控制  
X-Forwarded-For: HTTP的请求端真实的IP  
Request: 请求  
Response: 响应

**修改使用请求的<mark>方法</mark>，或者是用其他<mark>请求</mark>（一般默认是GET），直接在开头把GET改掉**
(目前只知道能在bp改，hackbar好像不行)

提到说要是admin，一般用bp，（hackbar感觉应该搞不了）
然后找user=这样的然后改成admin就行

**User-Agent**：WLLM
(可以修改打开的<mark>浏览器</mark>名称)
    (User- Agent中文名为用户代理，是一种向访问网站提供你所使用的浏览器类型及版本、操作系统及版本、[浏览器内核]、等信息的标识。)

```
User-Agent: <product> / <product-version> <comment>
```
web 浏览器的通用格式为：
```
User-Agent: Mozilla/5.0 (<system-information>) <platform> (<platform-details>) <extensions>
```

    <product>产品标识符——其名称或开发代号。
    <product-version>产品版本号。
    <comment>零个或多个包含更多细节的注释。例如，子产品的信息。

<mark>【所以原本格式是会带版本号之类的，要注意】</mark>

<mark>访问指定文件</mark>，记得看看**Location:** <mark>./ +文件名</mark>，可能过程中会*出现新文件*要修改文件名

**X-Forwarded-For**: 123.123.123.123
（XFF可以修改<mark>ip地址</mark>，可以搞个假的ip，应该也可以伪造代理服务器）

**Client-Ip** ：127.0.0.1
也可以修改<mark>ip地址</mark>，伪造

**Referer**: https://www.google.com/search?qiushuo
可以修改网址
描述是从什么什么来的，也是referer

    （在重放器请求包中添加IP地址 **一般情况下，X-Forwarded-For 字段应该被插入到 HTTP 请求头部的第一个位置**
    在 HTTP 请求头中，Referer 字段通常位于头部字段列表的第二个位置，紧随 Host 字段之后。）

**Via**：Clash.win
"Via"字段是一个可选的HTTP请求头字段，其中包含了<mark>代理服务器</mark>的相关信息。
    通过请求中的"Via"字段，可以获取有关请求的中间代理服务器的信息。

指出了 `localhost`，意在表明需要让服务器认为这是一个来自本地的请求。可以通过设置 `Host` `X-Real-IP` `X-Forwarded-For` `Referer` 等标头欺骗服务器。

以下任意一种请求都是可以的。
Host: localhost 
Referer: http://localhost

X-Real-IP: 127.0.0.1
X-Forwarded-For: 127.0.0.1
（这两个是一样的）

**Date**
　    　Date头域表示消息发送的时间，服务器响应中要包含这个头部，因为缓存在评估响应的新鲜度时要用到，其时间的描述格式由RFC822定义。
　例如，Date:Mon, 31 Dec 2001 04:25:57 GMT。Date描述的时间表示世界标准时，换算成本地时间，需要知道用户所在的时区。
　　
eg：Date: Sun,17 Sep 2019 02:01:16 GMT

`$b = @$_REQUEST['b'];`  

- **b=@b=@_REQUEST['b'];**：从请求中（可以是 GET 或 POST）获取名为 `'b'` 的参数，同样使用 `@` 符号来避免错误输出




###### GET 方法
_GET 用于从指定资源请求数据。_
_GET 是最常见的 HTTP 方法之一。_

请注意，查询字符串（名称/值对）是在 GET 请求的 URL 中发送的：
/test/demo_form.php==?name1=value1&name2=value2==
###### POST 方法

POST 用于将数据发送到服务器来创建/更新资源。

通过 POST 发送到服务器的数据存储在 HTTP 请求的请求主体中：
POST /test/demo_form.php HTTP/1.1
Host: w3school.com.cn
name1=value1&name2=value2

一般的直接修改，<mark>get，post，cookie这三个需要参数</mark>才能传参
get是？xx=1
post和cookie一样xx=1就行
所以要是题目没有给很明显的参数要自己去找去猜


<mark>字符    -    URL编码值</mark>

空格    -    %20  
"          -    %22  
#         -     %23  
<mark>%        -    %25</mark>  
&         -    %26  
(          -    %28  
)          -    %29  
加号     -    %2B  
,          -    %2C  
/          -    %2F  
:          -    %3A  
;          -    %3B  
<         -    %3C  
=         -    %3D  
    >        -    %3E  
?         -    %3F  
@       -    %40  
\          -    %5C  
|          -    %7C

#### 请求方式
     ctfhub某题

![在这里插入图片描述](https://i-blog.csdnimg.cn/blog_migrate/7a8dc3f7382dbe96b988de2f89c7a93b.png)  
HTTP方法是GET，使用CTFHUB方法，我会给你flag  
使用burpsuite[抓包](https://so.csdn.net/so/search?q=%E6%8A%93%E5%8C%85&spm=1001.2101.3001.7020)，修改请求方式GET为CTFHUB

GET请求方式在开头可以看见，GET /index.php   HTTP/1.1
直接把GET改为CTFHUB就可以，这也就是使用CTFHUB方法

![]({{ site.baseurl }}/assets/images/屏幕截图 2024-09-11 000034.png)



#### 看看你的HTTP
     basectf某题

按理来说用hackbar都可以完成（但是有的hackbar有bug）
GET好说 直接?basectf=we1c%2500me
POST好说 直接Base=fl@g
（火狐的这个好像不错所以可以Cookie这种，edge的好像不行得抓包）
这里Cookie，Referer，Use-Agent都可以传参，和POST一样方法直接输xx=xx


![]({{ site.baseurl }}/assets/images/屏幕截图 2024-09-21 145341 1.png)

然后ip就用抓包，X-Forwarded-For这个来修改ip伪造
直接在Host下一行加上X-Forwarded-For：127.0.0.1

![]({{ site.baseurl }}/assets/images/屏幕截图 2024-09-21 145413.png)

发送就能看见flag，不过需要解码问题不大

![]({{ site.baseurl }}/assets/images/Pasted image 20240921145822.png)

全用抓包的话
对POST方式，先要将数据包改成POST形式，就是开头把GET改错POST，再在请求头中加上Content-Type：application/x-www-form-urlencoded，再在请求头后空一行，写上Base=fl@g
（注意某些bp可能存在自动编码，需要删除最后的空行）

![]({{ site.baseurl }}/assets/images/屏幕截图 2024-09-22 150305.png)



**用bp在旁边修改的话**

    （旁边可以修改和添加）

GET传参不用别的，直接输入basectf=we1c%00me(这里不用转换url值，用hb需要把%改为%25)
<mark>POST传参</mark>也直接输，（在GET下面那个输入），按理来说会在左边最后一行出现传参的式子Base=fl@g（这里注意一下这个<mark>最后一行和上面之间要空一行</mark>，不能空很多）
，以及最开头的<mark>GET会变为POST</mark>（没有的话要自己改)
cookie直接改，User-Agent也是
Referer和X-forwarded-for没有找到的话，可以直接在左边输入xx：xx
也可以在右边的请求头里添加（下面有加号+可以添加请求头），然后输入



#### Do_you_know_http
     SWPUCTF某题

打开出现
Please use 'WLLM' browser!

browser 是计算机的浏览器，浏览的意思

所以意思应该是用叫WLLM的浏览器打开这个页面

把User- Agent改为WLLM
发现在Location处有一个a.php文件

    （Location这里格式是Location：./  +文件，前面的点杠都不是文件名（虽然误认了好像也没事））
     “./a.php”是一个相对路径，它指向当前目录下的一个名为“a.php”的文件。（一种解释）

![]({{ site.baseurl }}/assets/images/屏幕截图 2024-09-23 213601.png)

把hello.php修改a.php
得到
![]({{ site.baseurl }}/assets/images/屏幕截图 2024-09-23 213700.png)

题目回应You can only read this at local!
说你只能在本地读这个

所以应该是要修改数据包的源ip为我们本机的ip
（一般是指127.0.0.1）
   <mark>127.0.0.1是主机环回地址。</mark>
   
     127.0.0.1是回送地址，指本地机，一般用来测试使用。
所以修改ip用X-Forwarded-for
得到
![]({{ site.baseurl }}/assets/images/屏幕截图 2024-09-23 214140.png)

下面显示我的地址为127.0.0.1说明改了
然后发现Location这里又出现个文件
同理修改文件名
得到flag
![]({{ site.baseurl }}/assets/images/Pasted image 20240923220520.png)


//这题其实直接用hackbar更简单
就是要注意它load的网址和我直接页面的网址是不是一样的
![]({{ site.baseurl }}/assets/images/Pasted image 20241020100353.png)
这边直接在hackbar改就行了
它会自动跳转a.php不用上面bp手动修改
但是要注意自己的URL是不是一样的在a.php（刚刚就因为它还在hello那所以没有反应还以为坏了..）
然后改ip直接得到

#### Http pro max plus
     LitCTF某题（真的有点损，自行体会）

打开要修改地址，首先想到就是XFF，就用了得到
![]({{ site.baseurl }}/assets/images/屏幕截图 2024-09-23 222348.png)

(⊙﹏⊙)被狠狠嘲讽一通，好气哦，关键我还真的只知道XFF,更气了。然后去查（好吧还是看了wp）发现还可以用Client-Ip，是我浅薄了，涨知识了

然后要修改网址，再说要改浏览器（这里没截图，因为很正常.....）
然后就得到这个玩意，再一次被嘲讽了（实名怀疑是故意的
代理服务器之前没遇到过...查查说应该可以用XFF但是这里用了没反应，所以不行
继续查说用Via来伪造代理服务器


![]({{ site.baseurl }}/assets/images/屏幕截图 2024-09-23 222518.png)

之后叫我们借一步说话，应该是要访问指定文件，还给了文件名，所以直接在第一排添加文件名
最后得到这个，界面应该是有几个按钮的但是这里bp按不了
但是有源码，可以仔细瞅瞅
前面两个给的网址，我去一一打开了，没什么大用（点名批评第二个网站，竟然还是涩涩的，可恶）

看见最后面，发现有个文件名，所以应该也是要改成这个

![]({{ site.baseurl }}/assets/images/屏幕截图 2024-09-23 223620.png)


改完后得到flag


#### Interesting_http
    HNCTF 2022 某题

![]({{ site.baseurl }}/assets/images/屏幕截图 2024-09-25 130906.png)
提示 POST 发包，**参数名为 want** 。传参，a want，所以是一个
POST 发包 `want=1` ：
（或者是这样理解好像也对，post你想要的，所以是想要flag，直接want=flag，这样好像更好）

**No admin**
（这里也可能是英语语义有点小问题，但是可以猜测是用户）
应该是提示当前非 admin 用户。
bp抓包一下，找了半天用户在哪，终于在Cookie这里user看到了
直接修改为admin就好了
![]({{ site.baseurl }}/assets/images/屏幕截图 2024-09-25 131125.png)

然后得到
**Not location

（感觉应该是local，但是打成了location位置，emm）
猜测应该是非本地
所以是XFF

（主要是要猜测理解它让你干嘛，然后就很舒服）
