---
layout: post
title: pwn（hubu
date: 2024-12-17 22:50:16 +0800
categories: [pwn]
---

pwn

通过二进制漏洞获取计算机权限的方法或者过程被称为PWN。
在CTF中，PWN主要通过利用程序中的漏洞造成内存破坏以获取远程计算机的shell，从而获得flag。

PWN的做题步骤：
（1）checksec文件，了解文件保护开启信息
（2）运行文件，大概了解文件作用
（3）放到IDA中，详细观察文件的运行过程
（4）找到漏洞点
（5）思考攻击思路
ret2text，ret2syscall, ret2shellcode, ret2libc,........
（6）收集必要的信息
（7）编写exp，并且在编写的过程中不断使用GDB进行调试
（8）运行exp，达到攻击目的

ELF中的节用来存放各种各样不同的数据，主要包括：

❖ .text节——存放一个程序的运行所需的所有代码。
❖ .rdata节——存放程序使用到的不可修改的静态数据，如字符串等。
❖ .data节——存放程序可修改的数据，如C语言中已经初始化的全局变量等。
❖ .bss节——用于存放程序的可修改数据，与.data不同的是，这些数据没有被初始化，所以没有占用ELF空间。虽然在节头表中存在.bss节，但是文件中并没有对应的数据。在程序开始执行后，系统才会申请一块空内存来作为实际的.bss节。
❖ .plt节和.got节——程序调用动态链接库（SO文件）中函数时，需要这两个节配合，以获取被调用函数的地址。

关于整数的异常情况主要有三种：
溢出：
只有有符号数才会发生溢出。有符号数最高位表示符号，在两正或两负相加时，有可能改变符号位的值，产生溢出标志 OF 可检测有符号数的溢出，OF=1表示溢出，OF=0表示未溢出
回绕：
无符号数 0-1 时会变成最大的数，如 1 字节的无符号数会变为 255，而 255+1 会变成最小数 0。
进位标志 CF 可检测无符号数的回绕
截断：
将一个较大宽度的数存入一个宽度小的操作数中，高位发生截断
当无符号整型与有符号整型作比较时，无符号整形会变成有符号整型并且与当前比较值的符号相同

#### A_New_Shop
反编译ida64
找到主函数和其他

可以看到开始有100元
当money变为一个负数
然后会通过无负号整形unsigned int变为一个很大的数
shell是9999
这样就可以买到shell
所以关键在于要**使钱数变成负数**

（可以一个个输入怎么花100元得到负数就行）

shell得到
就可以输入指令
查看目录ls
然后看到flag

这个是vscode的呈现
![]({{ site.baseurl }}/assets/images/Pasted image 20241117105709.png)
这里是kali的


![]({{ site.baseurl }}/assets/images/Pasted image 20241117110717.png)
Stack Canary（金丝雀）
Stack Canary保护是专门针对栈溢出攻击设计的一种保护机制。由于栈溢出攻击的主要目标是通过溢出覆盖函数栈高位的返回地址，因此其思路是在函数开始执行前，即在返回地址前写入一个字长的随机数据，在函数返回前校验该值是否被改变，如果被改变，则认为是发生了栈溢出。程序会直接终止。
 GCC默认不开Stack Canary保护，关闭方法是在编译时加入“-
fno-stack-protector”参数。开启的方法是在编译时加入“ -fstack-protector-all”参数

PIE（程序地址随机化）（编译）
与ASLR保护十分相似，ASLR是指程序运行时的堆栈以及共享库的加载地址随机化；PIE保护的目的是让可执行程序ELF的地址
进行随机化加载，从而使得程序的内存结构对攻击者完全未知，进一步提高程序的安全性。

关闭ASLR：sudo sysctl -w kernel.randomize_va_space=0

GCC编译时开启PIE的方法为添加参数“-fpic-pie”。较新版本
GCC默认开启PIE，可以设置“-no-pie”来关闭。

Linux文件保护
Linux下的漏洞缓解措施

PIE（程序地址随机化）
ASLR


常见的危险函数如下：
❖ 输入： 
1.gets() ， 直接读取一行， 到换行符'\n' 为止， 同时'\n'被转换为'\x00’；
2.scanf()，格式化字符串中的%s不会检查长度；vscanf()，同上。

❖ 输出：sprintf()，将格式化后的内容写入缓冲区中，但是不检查缓冲区长度。

❖ 字符串：
1.strcpy()，遇到'\x00'停止，不会检查长度，经常容易出现单字节写0（off by one）溢出；
2.strcat()，同上。



CTFshow{Stack_Overflow}  






kali上面安装vscode（震惊


![]({{ site.baseurl }}/assets/images/Pasted image 20241117110204.png)



GDB
