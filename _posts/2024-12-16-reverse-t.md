---
layout: post
title: reverse(t
date: 2024-12-16 14:58:32 +0800
categories: [Reverse]
---

拖到IDA里面
右键可以看到text
找到main函数
tab和f5看源代码
空格回到开始界面

给出exe文件的话可以直接

加壳不影响程序的运行
但是拖到exenifo里面可能会看不到编译器之类的会出现unknown
拖到ida会发现可能没有main函数，函数很少


1.简单异或

#include <stdio.h>
#include <string.h>

int main(){
    char a[]="wwwwwww";
    char b[]="d`vxbQd";

    //try to find out the flag
    printf("please input flag:");
    scanf(" %s",&a);     //a是flag

    if(strlen(a)!=7){
        printf("NoNoNo\n");
        return 0;
    }

    for(int i=0;i<7;i++){
        a[i]++;
        a[i]=a[i]^2;     //这里是异或运算a[i]与2异或
    }

    if(!strcmp(a,b)){      //比较a和b的值，不一样输出good，说明a和b应该要一样
        printf("good!\n");
        return 0;
    }

    printf("NoNoNo\n");
    return 0;
    //flag 记得包上 NSSCTF{} 再提交!!!
}


所以脚本，直接把b[i]与2异或然后每次减一，然后输出得flag

![]({{ site.baseurl }}/assets/images/Pasted image 20241027104737.png)



2.





3.gamestart

jnz改为jz
看到failure


红色三角运行








#### replace
暴力破解
UPX是一种壳
UPX脱壳，把文件放在同一个目录下，然后upx.exe -d 加上文件名

![]({{ site.baseurl }}/assets/images/Pasted image 20241027162342.png)

![]({{ site.baseurl }}/assets/images/Pasted image 20241027162441.png)
int __cdecl main(int argc, const char **argv, const char **envp)
{
  char Buffer[40]; // [esp+4h] [ebp-2Ch] BYREF

  memset(Buffer, 0, sizeof(Buffer));
  printf("Welcome The System\nPlease Input Key:");
  gets_s(Buffer, 0x28u);
  if ( strlen(Buffer) - 35 <= 2 )  //长度小于等于37
  {
    if ( sub_401090(Buffer) == 1 )
      printf("Well Done!\n");
    else
      printf("Your Wrong!\n");
  }
  return 0;
}

int __fastcall sub_401090(int a1, int a2)
{
  int v4; // edx
  char v5; // al
  int v6; // esi
  int v7; // edi
  char v8; // al
  int v9; // eax
  char v10; // cl
  int v11; // eax
  int v12; // ecx

  if ( a2 != 35 )
    return -1;
  v4 = 0;     
  while ( 1 )
  {
    v5 = *(_BYTE *)(v4 + <mark>a1</mark>);   //首地址加偏移，可以看作寻址，a1是buffer
    v6 = (v5 >> 4) % 16;
    v7 = ((16 * v5) >> 4) % 16;   //右移4余16
    v8 = byte_402150[2 * v4];  //应该是一个数组
    if ( v8 < 48 || v8 > 57 )  
      v9 = v8 - 87;
    else
      v9 = v8 - 48;
    v10 = byte_402151[2 * v4];
    v11 = 16 * v9;
    if ( v10 < 48 || v10 > 57 )
      v12 = v10 - 87;
    else
      v12 = v10 - 48;
    if ( (unsigned __int8)byte_4021A0[16 * v6 + v7] != ((v11 + v12) ^ 0x19) )  //不想相等就跳过break，所以要相等
      break;
    if ( ==++v4 >= 35== )  //v4应该是计数的，每执行一次计数
      return 1;
  }
  return -1;
}



![]({{ site.baseurl }}/assets/images/Pasted image 20241027163152.png)

一般结尾要是0
看到这个最后才有0


到16h就停下，很长一段
全选然后右键convert python
会生成python列表


脚本

int __fastcall sub_401090(int a1, int a2)
{
  int v4; // edx
  char v5; // al
  int v6; // esi
  int v7; // edi
  char v8; // al
  int v9; // eax
  char v10; // cl
  int v11; // eax
  int v12; // ecx

  if ( a2 != 35 )
    return -1;
  v4 = 0;
  while ( 1 )
  {
    v5 = *(_BYTE *)(v4 + a1);
    v6 = (v5 >> 4) % 16;
    v7 = ((16 * v5) >> 4) % 16;
    v8 = byte_402150[2 * v4];
    if ( v8 < 48 || v8 > 57 )
      v9 = v8 - 87;
    else
      v9 = v8 - 48;
    v10 = byte_402151[2 * v4];
    v11 = 16 * v9;
    if ( v10 < 48 || v10 > 57 )
      v12 = v10 - 87;
    else
      v12 = v10 - 48;
    if ( (unsigned __int8)byte_4021A0[16 * v6 + v7] != ((v11 + v12) ^ 0x19) )
      break;
    if ( ++v4 >= 35 )
      return 1;
  }
  return -1;
}

int main （）{


for（int i =32;i<127;i++){
v6=(i>>)


}
}

//////








////
babyxor
脱壳

//
文件是什么100

写代码时某一行点了之后高亮就是下断点
按f8会继续向下执行

步过不会进入函数里面

步入会进入函数里面

pwngdb
pwndbg

改那个文件的路劲，前两个的
source /opt/gdb_plugins/pwndbg/gdbinit.py
群里有复制过来很奇怪

//////
ollvm

什么控制流平坦化
deflat-master

可以弄到kali

tab切换看到main函数的地址
////////

pyinsatller
pip install pyinsatller
python
cmd里面pyinsatller -F 加文件名会开始运行

python逆向pyc
放在同一个目录下，运行一条命令
python pyinstxtractor.py    加上exe文件名
![]({{ site.baseurl }}/assets/images/Pasted image 20241027172218.png)
会得到一个文件夹
![]({{ site.baseurl }}/assets/images/Pasted image 20241027172237.png)

处理.pyc文件
直接用一个命令
uncompyle6 pyc文件名
uncompyle6 -o py文件名 pyc文件名

或者取在线网站，注意改后缀

///////

安卓逆向分两层，java和原生层
用jakx打开apk文件
check函数在.so文件里
